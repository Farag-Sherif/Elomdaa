import React from "react";
import { multilanguage } from "redux-multilanguage";

const ReviewSection = () => {
  // الكومبوننت ده تم إيقافه (يرجع null) لإخفاء قسم التقييمات بالكامل
  return null; 
};

export default multilanguage(ReviewSection);